class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int p=0;
        int m=prices[0];
         for(int i=1;i<prices.size();i++){
             p=max(p,(prices[i]-m));
             m=min(m,prices[i]);

         }
         return p;
    }

};
